# Next

**Active phase:** none (M1 complete).

M1 (Settings/Preferences UI) is fully done: phase-01, phase-02, and phase-03
(slim down MenuBarView) have all landed. The MVP.md backlog items previously
queued as M2/M3/M4 candidates are also implemented:

- M2 candidate (UX): drop value in the nudge notification, device name in the
  connection status, and a launch-at-login toggle.
- M3 candidate: per-session goodSeconds + slouchEvents, persisted in daily
  history and shown live in the popover.
- M4 candidate: a live 60-second deviation chart and a named system-sound
  picker with preview.

No phase is currently drafted. Draft the next one with
`/rexymcp:architect next`.
